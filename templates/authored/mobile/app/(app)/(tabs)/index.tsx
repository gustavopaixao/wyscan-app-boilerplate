/**
 * Home tab.
 *
 * Renders the extracted welcome screen so the reference content survives the
 * navigation change and there is something real behind the chrome.
 */
import { WelcomeScreen } from "@/components/home/WelcomeScreen";

export default function HomeTab() {
	return <WelcomeScreen />;
}
