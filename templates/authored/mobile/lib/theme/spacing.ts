/**
 * Layout constants.
 *
 * Only what is genuinely shared. Screen-specific spacing stays in the screen's
 * own StyleSheet — a full spacing scale nobody follows is worse than none.
 */

/** Horizontal gutter for screen-level content. */
export const SCREEN_EDGE_PADDING = 16;

/** Minimum touch target, per the platform accessibility guidelines. */
export const MIN_TOUCH_TARGET = 44;

/** Gap between stacked buttons in a form. */
export const BUTTON_STACK_GAP = 12;
