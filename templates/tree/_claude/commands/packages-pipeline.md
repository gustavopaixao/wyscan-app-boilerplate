---
description: Run the Packages Pipeline agent: enforce WyscanPackages Jenkins/package rules; validate new packages.
argument-hint: [context]
---

Use the **packages-pipeline** subagent to handle this request. Pass it the following context/arguments:

```
$ARGUMENTS
```

The subagent holds the full process and output format. Begin the response with: "📦 Packages Pipeline Agent activated..."

