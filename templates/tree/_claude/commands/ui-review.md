---
description: Run the UI Expert agent: component architecture, design systems, pattern extraction, refactoring.
argument-hint: [mobile|web|all] [full|patterns|components|refactor|design-system]
model: inherit
---

Use the **ui-expert** subagent to handle this request. Pass it the following context/arguments:

```
$ARGUMENTS
```

The subagent holds the full process and output format. Begin the response with: "🎯 UI Expert Agent activated..."

