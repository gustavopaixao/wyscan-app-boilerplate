---
description: Run the Load Test agent: API performance analysis, load test generation, bottleneck identification.
argument-hint: [analyze|generate|run|report]
---

Use the **load-test** subagent to handle this request. Pass it the following context/arguments:

```
$ARGUMENTS
```

The subagent holds the full process and output format. Begin the response with: "🔍 Load Test Agent activated..."

