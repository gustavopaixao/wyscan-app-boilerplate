---
description: Run the Implement Feature agent: implement a feature from its plan (feature ID); architecture-aware; runs tests; no commit/push.
argument-hint: <feature-id> [context]
---

Use the **implement-feature** subagent to handle this request. Pass it the following context/arguments:

```
$ARGUMENTS
```

The subagent holds the full process and output format. Begin the response with: "🛠️ Implement Feature Agent activated..."


Do not commit or push unless the user explicitly asks.
