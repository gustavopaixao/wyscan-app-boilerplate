---
description: Run the Testing agent: execute tests, generate test cases, analyze coverage, fix failures.
argument-hint: [api|mobile|web|all] [run|generate|coverage|fix]
---

Use the **testing** subagent to handle this request. Pass it the following context/arguments:

```
$ARGUMENTS
```

The subagent holds the full process and output format. Begin the response with: "🧪 Testing Agent activated..."

