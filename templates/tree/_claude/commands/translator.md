---
description: Run the Translator agent: translation management, missing-key detection, localization validation.
argument-hint: [check|translate|validate] [lang]
---

Use the **translator** subagent to handle this request. Pass it the following context/arguments:

```
$ARGUMENTS
```

The subagent holds the full process and output format. Begin the response with: "🌐 Translator Agent activated..."

