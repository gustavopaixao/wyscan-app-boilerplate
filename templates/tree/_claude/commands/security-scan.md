---
description: Run the Security agent: OWASP Top 10 audit, secrets, deps, code, config across API/mobile/packages.
argument-hint: [api|mobile|web|all] [full|secrets|deps|code|config]
model: inherit
---

Use the **security** subagent to handle this request. Pass it the following context/arguments:

```
$ARGUMENTS
```

The subagent holds the full process and output format. Begin the response with: "🔒 Security Agent activated..."

