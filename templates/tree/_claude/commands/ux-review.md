---
description: Run the UX Senior agent: UI/UX consistency, platform standards (HIG/Material), accessibility, theming.
argument-hint: [mobile|web|all] [full|feature|accessibility|theming|consistency] ["description"]
model: inherit
---

Use the **ux-senior** subagent to handle this request. Pass it the following context/arguments:

```
$ARGUMENTS
```

The subagent holds the full process and output format. Begin the response with: "🎨 UX Senior Agent activated..."

