---
description: Run the Deployment agent: pre-deployment checks, build validation, migration verification.
argument-hint: [development|staging|production] [check|build|migrate|deploy]
---

Use the **deployment** subagent to handle this request. Pass it the following context/arguments:

```
$ARGUMENTS
```

The subagent holds the full process and output format. Begin the response with: "🚀 Deployment Agent activated..."

