---
description: Run the Feature Parity agent: mobile vs web parity, OpenAPI per package, UI/UX validation, gap plans.
argument-hint: [context]
---

Use the **feature-parity** subagent to handle this request. Pass it the following context/arguments:

```
$ARGUMENTS
```

The subagent holds the full process and output format. Begin the response with: "🔀 Feature Parity Agent activated..."

