---
description: Run the Code Review agent: security, performance, clean architecture, best practices, API/DTO contracts.
argument-hint: [api|mobile|web|all]
model: inherit
---

Use the **code-review** subagent to handle this request. Pass it the following context/arguments:

```
$ARGUMENTS
```

The subagent holds the full process and output format. Begin the response with: "🔍 Code Review Agent activated..."

