# __PROJECT_SLUG__-site

__PROJECT_NAME__ marketing site (Next.js, port 3500). Scaffolded by feature 0001.

- `pnpm dev` — dev server on http://localhost:3500 (Turbopack)
- `pnpm build` / `pnpm start` — production build / serve
- `pnpm lint` / `pnpm type-check` / `pnpm test`

## Environment (`.env.local`)

Create `web/__PROJECT_SLUG__-site/.env.local` (git-ignored) with:

```env
NEXT_PUBLIC_API_URL=http://localhost:8080
```
