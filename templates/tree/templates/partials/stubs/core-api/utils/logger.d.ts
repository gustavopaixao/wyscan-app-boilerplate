// STUB — replace with __NPM_SCOPE__/core-api when you adopt the shared packages.
type LogFn = (message: string, meta?: unknown) => void;
export declare const logger: { debug: LogFn; info: LogFn; warn: LogFn; error: LogFn };
